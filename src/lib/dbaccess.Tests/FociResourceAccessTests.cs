//using System;
//using System.Collections.Generic;
//using System.Linq;
//using Xunit;
//using dbaccess.Common;
//using dbaccess.Models;
//using dbaccess.Factory;
//using dbaccess.Mapper;
//using dbaccess.Repository;

//namespace dbaccess.Tests
//{
    ////public class FociResourceAccessTests
    ////{
        ////private readonly IFociResourceAccess _access;
        ////IAsqDbContextFactory<ASQContext> _contextFactory;

        ////public FociResourceAccessTests(IFociResourceAccess access, IAsqDbContextFactory<ASQContext> contextFactory)
        ////{
            ////this._access = access;
            ////this._contextFactory = contextFactory;
        ////}

        ////[Fact]
        ////public void ContextIsNotNull()
        ////{
            ////Assert.True(this._contextFactory.CreateContext() != null);
        ////}

        ////[Fact]
        ////public async void AllFociExist()
        ////{
            ////var foci = await this._access.GetFoci();

            ////Assert.True(foci != null);
            ////Assert.True(foci.Count() == 4);
        ////}

    ////}
//}
